server:
  port: 12000
spring:
  application:
    name: provider
  profiles:
    active: dev
  cloud:
    config:
      profiles: dev
      name: datasource, eureka-client, log
      uri: http://localhost:14000/
      label: master
      discovery:
        enabled: false
        service-id: ${spring.application.name}:${server.port}
        profile: dev
