server:
  port: 16000
spring:
  application:
    name: ribbon-client
  profiles:
    active: dev
  cloud:
    config:
      profiles: dev
      name: eureka-client, log
      uri: http://localhost:14000/
      label: master
