server:
  port: 11000
spring:
  application:
    name: provider
  profiles:
    active: dev
  cloud:
    config:
      fail-fast: true
      name: datasource, eureka-client, log
      uri: http://localhost:14000
