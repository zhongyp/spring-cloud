server:
  port: 10000
spring:
  application:
    name: eureka-server
  profiles:
    active: production
  cloud:
    config:
      fail-fast: true
      name: datasource, eureka-server, log
      uri: http://localhost:14000